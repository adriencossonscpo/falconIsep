package com.falcon.avisep.domain;

import org.hibernate.annotations.Cache;
import org.hibernate.annotations.CacheConcurrencyStrategy;

import javax.persistence.*;
import javax.validation.constraints.*;
import java.io.Serializable;
import java.util.Objects;

/**
 * A VTeacher.
 */
@Entity
@Table(name = "v_teacher")
@Cache(usage = CacheConcurrencyStrategy.NONSTRICT_READ_WRITE)
public class VTeacher implements Serializable {

    private static final long serialVersionUID = 1L;

    @Id
    @GeneratedValue(strategy = GenerationType.SEQUENCE, generator = "sequenceGenerator")
    @SequenceGenerator(name = "sequenceGenerator")
    private Long id;

    @Column(name = "vt_id")
    private Integer vtId;

    @OneToOne(optional = false)
    @NotNull
    @JoinColumn(unique = true)
    private Form form;

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    public Integer getVtId() {
        return vtId;
    }

    public VTeacher vtId(Integer vtId) {
        this.vtId = vtId;
        return this;
    }

    public void setVtId(Integer vtId) {
        this.vtId = vtId;
    }

    public Form getForm() {
        return form;
    }

    public VTeacher form(Form form) {
        this.form = form;
        return this;
    }

    public void setForm(Form form) {
        this.form = form;
    }

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        VTeacher vTeacher = (VTeacher) o;
        if (vTeacher.id == null || id == null) {
            return false;
        }
        return Objects.equals(id, vTeacher.id);
    }

    @Override
    public int hashCode() {
        return Objects.hashCode(id);
    }

    @Override
    public String toString() {
        return "VTeacher{" +
            "id=" + id +
            ", vtId='" + vtId + "'" +
            '}';
    }
}
